class dir:
    #path of the folder having train, test and validation datasets
    main_dir = '../'
    #path of the folder containing the training dataset
    train_dir = main_dir + '/train'
    #path of the folder containing the testing dataset
    test_dir = main_dir + '/test'


#class standard_vals:
class standard_vals:
    #height and width of the image
    height = 75
    width = 75
    #no. of channels of the image
    #(1 for grayscale, 3 for RBG, 4 for RBGA)
    channels = 3
